#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int x, y, res;
    int k;
    cin >> x; 
    k = sqrt(x);
    k = k - k % k;
    y = (k * (k + 1))*(2 * k + 1) / 3;
    res = (x *(x + 1)/2)- y;
    cout << res;
}
